# ── 팩-only 릴리스 레인 (2026-07-13 오너 승인 — "인테리어만 교체") ──
# 본체(바이너리) 버전 범프 없이 팩 콘텐츠만 전진시킨다. 사용자 GUI는 본체 알림 없이
# 팩 무중단(↻·세션/데몬 생존) 경로만 보게 된다.
#
# 사용법: 팩만 변경됐는지 scripts/release-lane-check.sh로 판별 후
#   git tag pack-vX.Y.Z && git push origin pack-vX.Y.Z
# 버전 규칙(충돌 함정): pack-vX.Y.Z 는 직전 pack_version(본체 결합값 포함)보다 커야 하고,
#   이후 '본체' 릴리스의 다음 버전은 반드시 최신 pack-v 버전보다 커야 한다(동일 pack_version
#   재사용 시 갱신 감지 무음 실패 — lane-check가 기계 검사).
#
# 게이트·산출물은 release.yml pack-artifacts 잡과 동형(시크릿 2중 스캔·역커버리지·동일성 3자
# 폐포·실서명→실검증). 차이는 ①pack_version을 태그에서 주입(--pack-version, 바이너리 버전과
# 분리) ②본체 자산 없이 팩 3종 + 직전 릴리스의 latest.json 캐리포워드(binary updater endpoint가
# releases/latest를 보므로, latest 마킹 후에도 본체 버전 감지가 끊기지 않게 유지).
name: pack-release

on:
  push:
    tags:
      - "pack-v*"

env:
  SRC_REPO: oogisoogi/cys-terminal   # 2026-09-08 포크 전환(TICKET=cys-fork-rebase-v0.14.30)

jobs:
  pack-only:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      KEY_ID: '54FBA04AD0E0F49D'    # 임베드 키링(cysjavis-pack/trusted-keys.json) key_id (2026-09-08 포크 전환)
      EXPIRES_DAYS: '90'            # 서명 신선도 유효창(release.yml과 동일)
      # ★min_binary는 정책 파생(해법③⑤ · 2026-07-14): scripts/min-binary-policy.sh가
      # max(security-floor.txt, 지원창 하한)을 계산 — 손설정 하한의 '잊음' 실패 계급 제거.
      #
      # ⚠**정책 스크립트가 모르는 것 = 기능 의존**(2026-07-26 S26 레인 비대칭 교정).
      #   min-binary-policy.sh 는 **보안 하한·지원창만** 계산한다. "이 팩이 새 바이너리 기능을
      #   요구하는가"는 그 입력에 아예 없다. 실측: `bash scripts/min-binary-policy.sh` → 0.12.48.
      #   그래서 **규칙**: 바이너리 릴리스가 새 CLI 표면(플래그·서브커맨드)을 추가하고 팩이 그것을
      #   **지시**하면, 다음 pack-v 태그 **전에** 아래 OVERRIDE 를 그 버전으로 올려라. 안 올리면
      #   구버전 바이너리 사용자가 팩을 받아 지시대로 실행했을 때 unknown-flag 로 죽는다.
      #   (같은 규칙의 본체 레인 대응물 = release.yml 의 `PACK_MIN_BINARY`. 두 레인은 **따로**
      #    고쳐야 한다 — 한쪽만 고치면 다른 문으로 같은 실패가 들어온다. 이게 S26 의 실제 사고다.)
      #
      # 현재 값 0.14.29 의 근거(2026-08-31 · T5T6 R1-적대 차단 수리 · master 결재): 0.14.29 팩의
      #   pack-guard.sh 가 "이 수정은 보존됩니다(kept-drift)" 를 약속하는데, 그 거동(수정 system
      #   파일의 벤더 미전진 보존·전진 시 자동 3-way)은 0.14.29 바이너리에서 처음 생긴다 —
      #   ≤0.14.28 스윕은 종전대로 치유해 팩 자신의 약속이 거짓이 된다(기능 의존).
      #   release.yml PACK_MIN_BINARY 와 동시 상향 — 두 값 동일 계약(RELEASE.md).
      #
      # 구 값 0.14.2 의 근거(이력): v0.14.2 팩의 javis_orchestra.py 가 `cys todo-path --emit-decl`
      #   실행을 지시하는데 그 플래그는 이 레인에서 v0.14.2 에 처음 생긴다(≤0.14.1 에는 부재 —
      #   `git show v0.14.1:src/bin/cys.rs | grep -c emit_decl` → 0 으로 실측).
      #   상향 시 release.yml 의 PACK_MIN_BINARY 와 **같은 값인지** 확인할 것.
      #
      # 값 지정 시 정책이 무시되고 "min_binary 정책 오버라이드 사용" ::warning 이 로그에 남는다
      #   (아래 Compute min_binary 스텝) — 의도된 흔적이다. 근거를 커밋에 남길 것.
      PACK_MIN_BINARY_OVERRIDE: '0.14.29'
    steps:
      - uses: actions/checkout@v4

      - name: Derive pack version from tag
        run: |
          set -euo pipefail
          PV="${GITHUB_REF_NAME#pack-v}"
          echo "$PV" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+$' || { echo "::error::태그가 pack-vX.Y.Z 형식이 아님: $GITHUB_REF_NAME"; exit 1; }
          echo "PACK_VERSION=$PV" >> "$GITHUB_ENV"
          echo "pack_version=$PV (tag $GITHUB_REF_NAME)"

      # ★해법③⑤: min_binary = max(security_floor, 지원창 하한) — 정책 공식(가시화 로그 포함).
      - name: Compute min_binary (policy)
        run: |
          set -euo pipefail
          git fetch --tags --quiet
          if [ -n "$PACK_MIN_BINARY_OVERRIDE" ]; then
            MB="$PACK_MIN_BINARY_OVERRIDE"
            echo "::warning::min_binary 정책 오버라이드 사용: $MB"
          else
            MB="$(bash scripts/min-binary-policy.sh)"
          fi
          echo "MIN_BINARY=$MB" >> "$GITHUB_ENV"
          echo "min_binary=$MB"

      # ★시크릿/개인정보 발행 hard-gate (release.yml과 동일 2중 스캔).
      - name: Secret/PII scan (pre-build hard-gate)
        run: bash scripts/secret-scan.sh --all

      # ★H-SECRET-1 — 스캐너가 **살아 있는가**를 증명하는 메타 게이트 (IG-2 · master 지시 2026-09-04).
      #   위 secret-scan.sh 는 트리가 깨끗하면 초록이다. 그런데 **스캐너가 죽어 있어도**(정규식
      #   파손·조기 exit 0·파일 목록 0건) 깨끗한 트리는 똑같이 초록이다 — 종료코드만 보는 게이트는
      #   자기 고장을 통과로 접는다. H-SECRET-1 은 규칙별 양성 표본을 임시 파일(저장소 밖)에 넣고
      #   **그 규칙 라벨로** 잡히는지까지 확인한다(라벨을 보는 이유: 아무 규칙이나 걸려도 exit 1 이라
      #   6종 중 5종이 죽어도 종료코드는 같다). 자기 면제 동결도 함께 본다.
      #   ★왜 지금 넣는가: 2026-09-04 팩 fixture 에 오너 핸들이 실제로 유출됐고(수리 011a6f6),
      #   그 계급을 잡는 규칙(handle→HANDLE)이 이 검체 안에 있다. 종전에 이 검체는 ci-branch
      #   잡2 에서만 돌았고 **발행 레인에는 0회** 였다 — "안 도는 검체는 게이트가 아니다".
      #   실측 22.1s(러너 자기완결·저장소 밖 임시파일·사용자 HOME 무접촉).
      - name: H-SECRET-1 — 시크릿 스캐너 계측 타당성 (발행 차단)
        shell: bash
        run: |
          set -euo pipefail
          R="cysjavis-pack/bin/tests/run_bootstrap_health.py"
          [ -f "$R" ] || { echo "::error::건강성 러너 누락: $R — 스캐너 계측 타당성 판정 불가(fail-closed)"; exit 1; }
          python3 "$R" --only H-SECRET-1

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          key: pack-release

      - name: Scan pack content (pre-build hard-gate)
        run: bash scripts/scan-pack-secrets.sh

      # ★★배송 팩 계약 게이트(C-1 세 번째 레인 교정 · 2026-07-26) — **이 레인도 pack.tar.gz 를 만들고
      #   pack-manifest.json 을 minisign 서명한다**(아래 `Identity gate + build pack.tar.gz` ·
      #   `Sign pack-manifest.json (minisign)`). 즉 사용자에게 배달되는 Python 선언 파서 본체가
      #   **파리티 검증 0회로 봉인되는 문**이 여기 열려 있었다.
      #   경위: C-1 을 release.yml 의 `pack-artifacts` 잡에만 걸고 닫았다고 판단했으나, 팩-only
      #   레인(pack-v* 태그)은 그 잡을 전혀 타지 않는다. S26(min_binary 하한을 release.yml 에만
      #   고치고 이 파일은 방치)과 **정확히 같은 형태의 레인 비대칭**이다 — 한 문을 닫고 "닫혔다"고
      #   적는 동안 다른 문이 열려 있었다. 그래서 이 게이트는 release.yml `pack-artifacts` 의
      #   동일 블록을 **축소 없이** 옮긴 것이다.
      # 위치: manifest emit(`Emit authoritative pack-manifest.json`)·tar 생성·minisign 서명
      #   **전부보다 앞**. 서명 뒤 검증은 이미 봉인된 것을 보는 일이라 의미가 없다.
      # 러너: ubuntu-latest. 위 `Setup Rust`(dtolnay stable)가 rustc 를 PATH 에 올려두므로 2언어
      #   파리티를 **여기서도 온전히** 돌린다(파이썬 단독 축소본 아님). 하네스는 rustc 부재 시
      #   hard fail 하고, 아래 `command -v rustc` 존재-단언이 그 앞에서 한 번 더 차단한다.
      # 존재-단언은 이 리포 관례(`[ -f "$f" ] || exit 1`)를 그대로 따른다 — untracked 누락으로
      #   게이트가 조용히 skip 되면 초록이 거짓말이 된다.
      - name: 배송 팩 계약 게이트 (선언 파서 2언어 파리티 + 유령집계 회귀)
        shell: bash
        run: |
          set -euo pipefail
          d="cysjavis-pack/bin/tests/fixtures/todo-decl"
          h="cysjavis-pack/bin/tests/parity_todo_decl.py"
          [ -d "$d" ] || { echo "골든 픽스처 디렉터리 누락: $d (계약 SOT 부재 = 실패)" >&2; exit 1; }
          [ -f "$d/expected.json" ] || { echo "계약 대장 누락: $d/expected.json (git add 누락 의심)" >&2; exit 1; }
          n="$(find "$d" -maxdepth 1 -name '*.md' | wc -l | tr -d ' ')"
          [ "$n" -ge 15 ] || { echo "픽스처 케이스 부족(${n}종 · 설계 P3-1 최소 15종)" >&2; exit 1; }
          [ -f "$h" ] || { echo "파리티 하네스 누락: $h (git add 누락 의심)" >&2; exit 1; }
          [ -f "scripts/todo_decl_dump.rs" ] || { echo "Rust 덤퍼 누락: scripts/todo_decl_dump.rs (git add 누락 의심)" >&2; exit 1; }
          [ -f "src/todo_decl.rs" ] || { echo "Rust 파서 누락: src/todo_decl.rs (git add 누락 의심)" >&2; exit 1; }
          command -v rustc >/dev/null || { echo "rustc 부재 — 2언어 파리티 불가(축소 실행 금지)" >&2; exit 1; }
          echo "== [pack-only] todo 선언 파서 2언어 파리티(골든 ${n}종 + 조합 1024건) =="
          if ! python3 "$h" --fuzz 1024 --list-mismatch 20; then
            echo "::error::[pack-only] 선언 파서 2언어 파리티 실패 — 배송될 Python 파서가 Rust 와 갈렸다. 서명 전 차단." >&2
            exit 1
          fi
          echo "== [pack-only] todo 선언·유령집계 소비자 회귀 7종(+import 가드) =="
          # ★W8(2026-08-16): v4-repair 스위트 3종 편입(3완전 레인 대칭 — ci-branch·release 와 동시).
          #   부서 이름검증·preflight C03 상태기계·CONTENT_PINS↔디렉티브 패리티 — cys-dept·preflight·
          #   디렉티브 전부 이 레인이 서명·배송하는 팩 내용물이라 서명 전 차단이 정위치다.
          # ★repair-w3(2026-08-29 · v0.14.28): 수리 회귀 핀 2종 편입 — test_hud_bridge_backoff
          #   (W2 replay_gap 재스폰 스톰 지수 백오프 — ★R2 라운드2에서 _reader 배선 통합 2건 추가)
          #   · test_wakeup_liveness(★R2 라운드2 신설 — _target_alive 3값 판정+drain zombie 가드
          #     행동 정본. 종전 유일 가드였던 rust substring 핀은 M8 변이에 초록이라 행동을 못
          #     지킨다 — lib.rs 역할 경계 주석) · test_preflight_c11b_seal(W3c 번들 봉인
          #   심링크 이관). 대상 javis_hud_bridge.py·javis_preflight.py 는 배송 팩 내용물인데
          #   두 핀 모두 어느 CI 레인에서도 0회 실행이었다("안 도는 검체는 게이트가 아니다" —
          #   W-CI 2026-08-21 동형 격차의 재발). 3완전 레인 동시·대칭 등재(release 는 양 루프) ·
          #   격리 스크래치 하네스(라이브 무접촉) · 실측 각 ≤0.1s.
          # ★2R-6(2026-08-31 · 0.14.29 성찰): test_preflight_c62_c68_ledger 편입 — T5 의 C62
          #   conflicted/quarantined 신설 판정·at-rest 병기와 T3 의 C68 영속 kind 기한 제외
          #   산식이 파이썬 검체 0건(무측정 변경)이던 격차 봉합. 픽스처 원장·뮤테이션
          #   양방향(PASS↔WARN) 검증 · 3완전 레인 동시·대칭 등재(release 는 양 루프) ·
          #   격리 CYS_PACK_DIR 하네스(라이브 무접촉) · 실측 0.01s.
          # ★C1(2026-09-04 · 부트 v2 W-C): test_runtime_seal 편입 — 동봉 런타임 봉인 매니페스트
          #   (javis_runtime_seal.py)의 회귀 핀. 대상 모듈은 배송 팩 내용물이고, Windows 는
          #   코드서명 sealed-resource 봉인이 없어 **설치 후 runtime/** 오염을 잡을 유일한 수단**이다.
          #   봉인 축 11개(추가·삭제·내용변경·심링크↔실복사본 양방향·실행비트·미추적·결정론·
          #   판정불가 3종·자기제외) · 뮤테이션 5종 RED 실측으로 게이트 실효 확인 ·
          #   3완전 레인 동시·대칭 등재(release 는 양 루프) · 격리 tempdir 하네스(라이브 무접촉) · 실측 0.9s.
          for t in test_todo_decl test_todo_stamp test_report_ghost test_hud_bridge_skew \
                   test_report_gate test_todo_shared_constants test_import_guard test_radio test_scrub \
                   test_bootstrap_chain test_snapshot \
                   test_dept_name_guard test_preflight_c03_states test_content_pins_parity \
                   test_role_bootstrap_hook test_seat_latch_negation \
                   test_hud_bridge_backoff test_preflight_c11b_seal test_wakeup_liveness \
                   test_preflight_c62_c68_ledger \
                   test_runtime_seal test_preflight_c80_runtime_seal test_runtime_manifest_parity \
                   test_check_ack_addr_gate test_hook_launcher_split test_mission_origin_parity \
                   test_bootlast_golden_lock_parity test_bootv2_doc_contract test_pyseal_census \
                   test_sandbox_state_isolation test_settings_torn_write test_state_snapshot_root \
                   test_preflight_hook_body test_preflight_npm_prefix test_learn_digest_queue \
                   test_participant_formation test_formation; do
            f="cysjavis-pack/bin/tests/$t.py"
            [ -f "$f" ] || { echo "선언·유령집계 테스트 파일 누락: $f (git add 누락 의심)" >&2; exit 1; }
            echo "== [pack-only] declared-state test: $t =="
            if ! CYS_PACK_DIR="$(mktemp -d)" python3 "$f"; then
              echo "::error::[pack-only] 선언·유령집계 게이트 실패($t) — 배송 팩 회귀. 서명 전 차단." >&2
              exit 1
            fi
          done
          # ★W-CI(2026-08-21): Wave 1 부트 결정론 회귀 핀 2종 편입(3완전 레인 동시·대칭 —
          #   ci-branch·release 와 같은 커밋). 팩-only 레인도 같은 훅·감지기를 서명·배송한다 —
          #   W8 때와 같은 사유로 서명 전 차단이 정위치(레인 하나 빼먹기가 이 리포의 반복 사고 형태).
          #   ① test_role_bootstrap_hook(위 루프 말미): _notify_bg 파이프 해제·≤0.3s 정렬 창·발화
          #      행렬 핀 — 실측 약 45s(내부 subprocess timeout 20~30s 유계가 상한).
          #   ② 감지기 self-test(실측 0.03s): 4축 억제 corpus + 단일 원본 fixture 로딩 검증.
          echo "== [pack-only] javis_detect 4축 감지기 self-test(corpus 단일 원본) =="
          g="cysjavis-pack/bin/javis_detect.py"
          [ -f "$g" ] || { echo "감지기 누락: $g (git add 누락 의심)" >&2; exit 1; }
          if ! CYS_PACK_DIR="$(mktemp -d)" python3 "$g" --self-test; then
            echo "::error::[pack-only] javis_detect self-test 실패 — 감지기 4축 억제 회귀. 서명 전 차단." >&2
            exit 1
          fi
          # ★W-CI2(2026-08-21): Wave 2 좌석 래치 회귀 핀 test_seat_latch_negation 편입(위 루프
          #   말미 · 실측 0.007s·19케이스 · 순수 픽스처·라이브 무접촉 — ci-branch·release 와
          #   같은 커밋·3완전 레인 동시 대칭). 판정 원본 javis_orchestra.py·javis_boot_node.py
          #   는 이 레인이 서명·배송하는 팩 내용물 — W8·W-CI 와 같은 사유로 서명 전 차단이
          #   정위치다(레인 하나 빼먹기가 이 리포의 반복 사고 형태). 봉인: awakened_at 래치
          #   부정 3중 AND + ★agent_alive:null(Windows master) 래치 유지 — 낙관적 falsy 역수리 =
          #   Windows 전 pane 사망(치명 앵커 ④) 재발 경로를 서명 전에 차단한다.
          # ★todo 스캔 집합 파리티(S18) — 파서 파리티와 다른 표면("같은 파일을 보는가").
          echo "== [pack-only] todo 스캔 집합 2언어 파리티(C1·C2 동일 파일 집합) =="
          hs="cysjavis-pack/bin/tests/parity_todo_scan.py"
          [ -f "$hs" ] || { echo "스캔 파리티 하네스 누락: $hs (git add 누락 의심)" >&2; exit 1; }
          [ -f "scripts/todo_scan_dump.rs" ] || { echo "Rust 덤퍼 누락: scripts/todo_scan_dump.rs (git add 누락 의심)" >&2; exit 1; }
          [ -f "src/todo_scan.rs" ] || { echo "스캔 단일 구현 누락: src/todo_scan.rs (git add 누락 의심)" >&2; exit 1; }
          if ! python3 "$hs"; then
            echo "::error::[pack-only] todo 스캔 집합 파리티 실패 — C1(보고기)과 C2(데몬)가 다른 파일을 본다. 서명 전 차단." >&2
            exit 1
          fi

      # ★cargo clean — 스테일 OUT_DIR/pack_all.rs(이전 트리 임베드 테이블) 재사용이 manifest SOT를
      # 오염시키는 회귀 차단(release.yml과 동일).
      - name: cargo clean (스테일 생성표 무효화)
        run: cargo clean

      - name: Build cys
        run: cargo build --release --bin cys

      # 권위 manifest — pack_version은 태그 주입(팩-only 레인 핵심), 나머지는 release.yml과 동일.
      - name: Emit authoritative pack-manifest.json (pack_version = tag)
        run: |
          set -euo pipefail
          SIGNED_AT="$(date -u +%s)"
          EXPIRES_AT="$(( SIGNED_AT + EXPIRES_DAYS * 86400 ))"
          ./target/release/cys pack-manifest \
            --key-id "$KEY_ID" \
            --signed-at "$SIGNED_AT" \
            --expires-at "$EXPIRES_AT" \
            --min-binary-version "$MIN_BINARY" \
            --pack-version "$PACK_VERSION" \
            > pack-manifest.json
          python3 -c "import json; m=json.load(open('pack-manifest.json')); assert m['pack_version']=='$PACK_VERSION', m['pack_version']; print('pack_version =', m['pack_version'], '· files =', len(m['files']))"

      # ★역방향 커버리지 게이트(release.yml과 동형 — SOT=manifest, 잔차만 분류).
      - name: Reverse coverage gate (disk orphan detection)
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json, subprocess, sys
          man = json.load(open("pack-manifest.json"))
          embedded = set(man["files"])
          if not embedded:
              print("::error::manifest.files 비어있음 — 역커버리지 불가", file=sys.stderr); sys.exit(1)
          out = subprocess.run(["git", "ls-files", "cysjavis-pack"],
                               capture_output=True, text=True, check=True).stdout
          tracked = set()
          for line in out.splitlines():
              line = line.strip()
              if line.startswith("cysjavis-pack/"):
                  tracked.add(line[len("cysjavis-pack/"):])
          if not tracked:
              print("::error::git ls-files cysjavis-pack 비어있음 — 인덱스 부재?", file=sys.stderr); sys.exit(1)
          def is_excluded(rel):
              return any(c.startswith(".") or c == "tests" or c == "__pycache__"
                         for c in rel.split("/"))
          gap = tracked - embedded
          orphans = sorted(g for g in gap if not is_excluded(g))
          if orphans:
              print("::error::디스크 고아(git-추적이나 임베드 누락):", file=sys.stderr)
              for o in orphans:
                  print(f"::error::  {o}", file=sys.stderr)
              sys.exit(1)
          print(f"[역커버리지] tracked={len(tracked)} embedded={len(embedded)} "
                f"gap={len(gap)}(전부 제외규칙) orphan=0 — 통과")
          PY

      # ★embed-vs-standalone 동일성 게이트 + 결정론 pack.tar.gz (release.yml과 동형 3자 폐포).
      - name: Identity gate + build pack.tar.gz
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import hashlib, json, os, subprocess, sys, tempfile
          ROOT = "cysjavis-pack"
          man = json.load(open("pack-manifest.json"))
          files = man["files"]
          if not files:
              print("::error::manifest.files 비어있음", file=sys.stderr); sys.exit(1)
          def sha256_file(p):
              h = hashlib.sha256()
              with open(p, "rb") as f:
                  for chunk in iter(lambda: f.read(1 << 16), b""):
                      h.update(chunk)
              return h.hexdigest()
          for rel, want in files.items():
              src = os.path.join(ROOT, rel)
              if not os.path.isfile(src):
                  print(f"::error::manifest 등재 파일이 트리에 없음: {rel}", file=sys.stderr); sys.exit(1)
              got = sha256_file(src)
              if got != want:
                  print(f"::error::동일성 불일치 {rel}: embed {want} != tree {got}", file=sys.stderr); sys.exit(1)
          print(f"[gate①] embed-vs-tree {len(files)}개 sha256 전수 일치")
          with open("pack-files.txt", "w") as f:
              for rel in sorted(files):
                  f.write(rel + "\n")
          subprocess.run([
              "tar", "--sort=name", "--owner=0", "--group=0", "--numeric-owner",
              "--mtime=UTC 2020-01-01", "-czf", "pack.tar.gz",
              "-C", ROOT, "-T", "pack-files.txt",
          ], check=True)
          with tempfile.TemporaryDirectory() as td:
              subprocess.run(["tar", "-xzf", "pack.tar.gz", "-C", td], check=True)
              extracted = {}
              for dirpath, _dirs, names in os.walk(td):
                  for n in names:
                      p = os.path.join(dirpath, n)
                      extracted[os.path.relpath(p, td)] = sha256_file(p)
              if set(extracted) != set(files):
                  miss = set(files) - set(extracted)
                  extra = set(extracted) - set(files)
                  print(f"::error::tar⇔manifest 집합 불일치 missing={sorted(miss)} extra={sorted(extra)}", file=sys.stderr)
                  sys.exit(1)
              for rel, want in files.items():
                  if extracted[rel] != want:
                      print(f"::error::tar sha256 불일치 {rel}", file=sys.stderr); sys.exit(1)
          print(f"[gate③] tar⇔manifest⇔embed 3자 폐포 {len(files)}개 일치")
          man["digest"] = sha256_file("pack.tar.gz")
          with open("pack-manifest.json", "w") as mf:
              json.dump(man, mf, ensure_ascii=False, indent=2)
              mf.write("\n")
          print(f"[gate④] digest 주입 → {man['digest'][:16]}…")
          PY

      - name: Scan pack content (pre-publish hard-gate)
        run: bash scripts/scan-pack-secrets.sh

      - name: Sign pack-manifest.json (minisign)
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          test -n "${TAURI_SIGNING_PRIVATE_KEY:-}" || { echo "::error::TAURI_SIGNING_PRIVATE_KEY 미설정"; exit 1; }
          bunx @tauri-apps/cli@2 signer sign \
            --private-key "$TAURI_SIGNING_PRIVATE_KEY" \
            --password "" \
            pack-manifest.json
          base64 --decode pack-manifest.json.sig > pack-manifest.json.minisig

      # ★실서명→실검증 폐포(release.yml과 동일 — 배포 형식이 실검증기를 통과해야 발행).
      - name: Verify signed pack end-to-end
        run: |
          set -euo pipefail
          ./target/release/cys pack-update --from . --dry-run
          echo "✅ 실서명 아티팩트 실검증 통과(최신 검증기)"

      # ★해법②: '하한 호환' 선언을 매 릴리스 기계 증명 — min_binary '실물' 검증기로도 통과해야
      # 발행. 하한 태그 소스를 빌드(최초 1회 비용)하고 이후는 바이너리 캐시로 즉시.
      - name: Cache min_binary verifier
        uses: actions/cache@v4
        with:
          path: .minbin-cache
          key: minbin-${{ env.MIN_BINARY }}-${{ runner.os }}

      - name: Verify with min_binary verifier (해법②)
        run: |
          set -euo pipefail
          BIN=".minbin-cache/cys-v$MIN_BINARY"
          if [ ! -x "$BIN" ]; then
            echo "하한 검증기 캐시 미스 — v$MIN_BINARY 소스 빌드(최초 1회)"
            git fetch --depth 1 origin tag "v$MIN_BINARY"
            git worktree add /tmp/minbin "v$MIN_BINARY"
            (cd /tmp/minbin && cargo build --release --bin cys)
            mkdir -p .minbin-cache && cp /tmp/minbin/target/release/cys "$BIN"
            git worktree remove --force /tmp/minbin
          fi
          "$BIN" pack-update --from . --dry-run
          echo "✅ 하한(v$MIN_BINARY) 검증기로도 통과 — min_binary 호환이 선언이 아니라 증명됨"

      # 직전 latest 릴리스의 latest.json 캐리포워드 — binary updater endpoint(releases/latest/
      # download/latest.json)가 이 릴리스를 latest로 봐도 본체 버전 감지가 유지되게 한다.
      # (latest.json 내 다운로드 URL은 절대경로라 원 릴리스 자산을 계속 가리킨다 — 유효.)
      - name: Carry forward latest.json from previous release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          PREV=$(gh release list -R "$SRC_REPO" --exclude-drafts --exclude-pre-releases --limit 20 \
            --json tagName --jq '[.[] | select(.tagName | startswith("pack-v") | not)][0].tagName')
          test -n "$PREV" || { echo "::error::직전 본체 릴리스를 찾지 못함 — latest.json 캐리포워드 불가"; exit 1; }
          gh release download "$PREV" -R "$SRC_REPO" --pattern latest.json
          python3 -c "import json; d=json.load(open('latest.json')); print('carry-forward latest.json: binary', d.get('version'), 'from', '$PREV')"

      - name: Create pack release + upload assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          # ★원작자 표기 고정 블록(박사님 지시 2026-09-10 · 원작자 통화 허락 조건 = 최초 개발자 명시).
          #   본문을 파일로 조립하는 이유: `--notes` 인라인은 줄바꿈을 못 실어 블록이 한 줄로 뭉개진다.
          #   문안 정본 = README「원작자」절 · 검사 축 = test_participant_formation.py ⓕ(idoforgod 부재=적색).
          #   heredoc 은 비인용(<<NOTES)이라 $MIN_BINARY 가 펴진다 — 본문에 백틱·타 변수는 없다.
          cat > release-notes.md <<NOTES
          팩-only 릴리스 — 본체 버전 불변(min_binary $MIN_BINARY+ 무중단 적용). latest.json은 직전 본체 릴리스 캐리포워드.

          ---
          cys 터미널의 원작자는 CYSJavis(GitHub: idoforgod)입니다. 이 배포본은 원작자의 허락을 받아 oogisoogi가 원작(MIT)을 바탕으로 빌드·서명·배포하는 파생판입니다. 원작 저장소: https://github.com/idoforgod/cys-terminal
          NOTES
          gh release create "$GITHUB_REF_NAME" -R "$SRC_REPO" \
            --title "pack $PACK_VERSION (팩-only 무중단)" \
            --notes-file release-notes.md \
            pack.tar.gz pack-manifest.json pack-manifest.json.minisig latest.json
          gh release edit "$GITHUB_REF_NAME" -R "$SRC_REPO" --latest --draft=false
          echo "✅ pack-only 릴리스 발행: $GITHUB_REF_NAME (pack_version=$PACK_VERSION)"
