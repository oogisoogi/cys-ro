# 이 폴더의 역할

- 이 폴더에서 실행되는 에이전트의 역할은 **{{ROLE}}**입니다.
- 역할 지침의 원본은 `~/.cys/pack/directives/{{DIRECTIVE}}`에 있습니다. 세부 규칙은 그 파일을 따르세요.
- 마스터에게 보낼 때는 화면 번호가 아니라 역할 주소를 씁니다: `cys send --to master "..."` (화면 번호는 다시 켜면 바뀝니다).
- 이 파일은 cys가 이 폴더를 처음 만들 때 한 번 만들었습니다. 내용을 고쳐도 다시 덮어쓰지 않습니다.
