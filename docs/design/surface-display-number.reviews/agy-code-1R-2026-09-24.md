VERDICT: ACCEPT
FINDINGS: 없음

T-A 부팅 순서: 「성립 + 파일 유무 기록 후 open_db로 스키마를 세우고, 3갈래 질의가 독립적으로 수행되며 고아 UPDATE 실패 시에도 스냅샷 결과를 보존하고 버스 생성 직후 경보를 발행함」
T-B 락 범위: 「성립 + display_alloc 안에서 fetch_add만 수행하고 DB 쓰기나 타 락 획득이 없으며, surfaces 락과 교착을 일으키지 않고 guard 해제 위치가 정확함」
T-C 경보 식별: 「성립 + kind 5종 및 surface_id 유무가 설계대로 분리되었으며, 진입/발동 시점에만 1회 경보를 내고 pk_conflict 시 이후 UPDATE를 차단함」

그 밖에:
- 보이는 번호가 파괴/입력 RPC로 유입되어 다른 좌석에 닿는 경로: 없음 (데몬 내부용 `parse_surface_ref`가 무변경으로 유지되어 `#N` 문법을 원천 거부함).
- 좌석 생성이 막히는 새 경로: 없음 (대응표 INSERT가 `pk_conflict`/`write_io`로 실패해도 에러를 삼키고 경보만 발생시킨 뒤 `openpty` 생성을 속행하며, 다 찬 상태에서도 `None`을 부여해 좌석을 만듦).
