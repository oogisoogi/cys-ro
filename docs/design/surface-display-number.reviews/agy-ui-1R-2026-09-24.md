VERDICT: REVISE
FINDINGS: 2
[HIGH] [ui/src/panetitle.ts:ruleTitleOf] 이름 변경 되돌리기 시 사람 이름 오인: `ruleTitleOf`가 단순히 `startsWith(보이는 번호 + " · ")`로 검사하므로, 사용자가 "50 · 내 이름"과 같이 번호를 살려 수동으로 창 이름을 변경하면 이를 초기 규칙 제목으로 오인함. 이후 빈 이름 확정(되돌리기) 시 본래의 "50 · worker" 등이 아닌 사용자가 입력했던 "50 · 내 이름"으로 잘못 덮어쓰게 됨(표적 ⑷).
[MED] [ui/src/alertcopy.test.ts, ui/src/panetitle.test.ts:배선 시험] 공허 단언(Empty Assertion) 사용: `main.ts`를 텍스트로 읽어 `expect(main).toContain(...)`으로 하드코딩된 코드 조각을 검사하고 있음. 코드가 논리적으로 망가지거나 주석 처리되어도 문자열만 존재하면 테스트가 통과하여 뮤턴트를 잡지 못하며, 반대로 공백이나 변수명 하나만 바뀌어도 테스트가 깨짐(표적 ⑸).

---
## 처리(worker-31 · 2026-09-24)
- [HIGH] ruleTitleOf 접두 오인 — **기존 동작 · 이 티켓 범위 밖(외과적 변경)**. 근거: T-UI 원본 `git show 3bc73856:ui/src/panetitle.ts` 46행이 같은 판정(`title.startsWith(\`${sid}${SEP}\`)`)이다 — v116-num 은 번호 종류(sid → display_no)만 바꿨고 오인 가능성은 늘지도 줄지도 않는다. master 에 T-UI 관찰 사항으로 상신(고치려면 데몬 규칙 제목 = role 특성과의 완전 일치 판정 등 T-UI 소유 설계 결정).
- [MED] 배선 소스 문자열 핀 — **기각(사유)**: 저장소 관례(T-UI 자신의 배선 핀과 같은 방식)이고, 판정의 주 시험은 순수 함수 행동 시험이다(M17 = titleNo 가 내부 번호를 돌려주는 뮤턴트를 행동 시험 3건이 잡았다). 핀은 「호출부가 새 인자를 넘긴다」 한 가지만 고정하는 보조다.
