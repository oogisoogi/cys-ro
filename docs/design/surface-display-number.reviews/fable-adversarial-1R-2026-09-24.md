# Fable 적대 서브에이전트 1R — 판정 원문 (설계 초안 1f045cee 대상)

- 발주: worker-31@surface:1049 · 2026-09-24 07:58 · 모델 fable(서브에이전트 · 읽기 전용 · 도구 14회 · 약 474초)
- 조준: 「보이는 번호가 destructive 경로로 새어 산 좌석을 닫을 수 있는가」(4군 ④)
- 아래는 서브에이전트 최종 보고를 **그대로** 옮긴 것이다(HTML 엔티티 `&amp;`·`&lt;` 만 원래 글자로 되돌림).

---

VERDICT: **REVISE** (primary target — a display number reaching a destructive path and hitting a *different* live pane — not found; but two claims the doc states as absolute are false, and one unspecified detail breaks pack boot parsers).

## Primary target (4군 ④): no cross-pane leak found — what was checked

- [VERIFIED lib.rs:2723-2727] `parse_surface_ref` = trim → strip `surface:` → `u64` parse. `#17` → `None`. Unchanged per doc.
- [VERIFIED handlers.rs:318-324] `resolve_surface_id` accepts JSON number or string via `parse_surface_ref` only. All destructive RPCs go through it: `surface.send_text` :3558/3559, `send_key` :3932/3933, `surface.close` :4253/4254, `surface.reap` :4334/4335, `surface.attach` :5193/5194, `queue.clear` :7394/7395, `queue.deliver` :7476/7486. Other `surface_id` parses (:5270 ledger, :5460 feed.push, :5795 recall query) are non-destructive. `schedule.rs:1191` parses `surface:N` from launch-agent stdout (see LOW-7).
- [VERIFIED] Surface creation: only production `Surface {` construction is state.rs:3692 inside `create_surface_with_env` (:3483; `fetch_add` :3502; env `CYS_SURFACE_ID` = bare `id.to_string()` :3609; map insert :3823). All other `.insert(s.id…)` sites (state.rs 6730+, governance.rs 6349+, boot_supervisor 5502+) are inside `#[cfg(test)]` modules (state `mod tests` :5017, governance :6283, boot_supervisor :2919). `create_surface` :3467 is the wrapper.
- [VERIFIED] Removal: only `surfaces.remove(&id)` is governance.rs:4891 in `close_surface` :4885.
- [VERIFIED] Daemon restart: no hot-handoff/seamless path (grep `handoff|seamless|SCM_RIGHTS|reexec|takeover|adopt` in main.rs/state.rs/governance.rs → only `takeover_empty_seat` restore, which creates NEW surfaces). `shutdown_cleanup` main.rs:1386; PTY children die with daemon (state.rs:20-21 setsid+killpg, Windows Job :3657). So "boot orphan" semantics and I1 after restart hold.
- [VERIFIED] Title parsing: panetitle.rs `starts_with_number` :182, `stale_number_tail` :234, `retitle_with_model` :57 only produce title text; callers handlers.rs:3320 (initial_title) and :6547 (retitle) never feed the parsed number back as a surface id. `src-tauri/src/main.rs`: no title-number parse (grep parse::<u64>/split on ` · ` → 0). ui/src/main.ts: `isAutoTitle` :2396 matches `surface \d+` only; GUI close/transfer use `dataset.sid` (:3458-3484). Pack: only `javis_panetitle.py` :123/:132 parses numbers, and its rename uses `surface_id` from `surface.list` (:62-70), not the title.
- [VERIFIED] CLI: every `surface: Option<String>` is `#[arg(long)]` (cys.rs 65-1354, 33 sites); positional `surface: String` are `CloseSurface` :318, `ReapSurface` :330, `Attach` :365, queue `Deliver` :845 — all required. `master-send.sh:93` accepts only `^surface:[0-9]+$` (rejects `#N`/bare).
- Corollary S [INFERENCE, proof checked]: holds per daemon. I1 protection ④ is unreachable in an I0-intact daemon (999 blocked numbers need 999 owners with ids < id ≤ 999).

## FINDINGS

**[MED] §4-2 / R-12 — "환경변수 폴백으로 새지 않는다" is false for flag-after-positional ordering.** Shell comment eats `#17` *and everything after it*, so `--surface` disappears only if it is the last flag. `cys send hello --surface #17` and `cys send-key Return --surface #17` are valid clap orderings (`text`/`keys` are positional: cys.rs:86-89, :100-101) → after stripping: `cys send hello` → `target_surface` env fallback [VERIFIED cys.rs:1434-1438] → text/Return injected into the **caller's own live pane**. Same for `feed push` (:3903-3906), `run` (:19265-19266), `quiesce` (None = self). Master (cmux pane) has no `CYS_SURFACE_ID` (:13629) → error; but every cys-launched worker/CSO does (state.rs:3609), and Claude Code's Bash tool is non-interactive bash where `#` comments are always active. Not another pane, but an unintended live pane and a false absolute claim. — fix: (a) make `--surface=#17` (equals form) the only documented spelling everywhere (hooks/directives/master rule), and correct R-12 to "오류 **또는** 자기 좌석 주입(폴백)"; (b) T9 adds the `=`-form case and a flag-after-positional case asserting the observed behavior; (c) 📌 owner option: accept a shell-safe alias (e.g. `n17`) in `parse_display_ref` alongside `#17`.

**[MED] §4-1 `cys list` "한 열" — column position unspecified, and pack parsers are positional.** [VERIFIED] `cys list` TSV = `surface_ref\trole=\tpid=\texited=\ttitle\tcwd` (cys.rs:3018-3030; no `--json`, :63). Consumers: `javis_boot_node.py:271-289` requires `cols[0].startswith("surface:")`; `javis_orchestra.py:2835-2857` uses `f[0],f[1],f[3]`; `javis_awaken.py:209-223` takes `cols[-1]` as cwd when it has no `=`; `cys-dept:884-895` uses `f[1],f[3]`. A leading column silently yields `[]` → boot_node's `surface_occupied` false → duplicate agent launch (4군 ③-adjacent); a trailing bare column corrupts awaken's cwd. — fix: specify the token as `key=value` (`no=50` / `no=-`) inserted **after `exited=` and before title**, or JSON-only (`surface.list.display_no`) with title already carrying the number; add a pack test that the four parsers still return rows.

**[MED] §8 / R-7 — bare-number collision across sockets is *newly* possible; S is per-daemon only.** Today HQ ids are ≥1000 and dept ids ≤30, so a human number can't hit the wrong daemon's seat. After the change, HQ `#17` (= surface:1016) and dept internal/display 17 coexist; `cys close-surface 17` or `#17` run with `CYS_SOCKET` = dept (any dept worker pane) closes the dept seat. Doc calls this "기계적으로 모호하지 않다" — true per socket, misleading for the operator. — fix: `surface.resolve_display` returns `socket`; CLI prints `#17 → surface:1016 @<dept|hq>` on stderr before destructive commands; state in §2-3 that S/I1 are per-daemon; keep the dept-prefix naming rule.

**[LOW] §6 / I0 — seed silently degrades to 0 on DB failure.** [VERIFIED recall.rs:387-408] any open/query error → `unwrap_or(0)` → next_id=1 → I0 broken; the design's INSERT then hits a PK conflict and lands in §3-2 ⑤ with a misleading "disk full" wording. — fix: distinguish `pk_conflict` (I0 violation) from I/O failure in `surface.numbers_write_failed`; boot with unreadable DB emits its own alarm; T2 adds the mutant.

**[LOW] §3-2 — crash between `fetch_add` and INSERT** consumes an id that was never handed out; it is reissued at next boot (harmless, but M11's "PTY 뒤로" mutant should be distinguished from this accepted case). — fix: one line in §3-2 stating this window is accepted and why.

**[LOW] §8 — two operator tools reject/ignore display numbers and the doc should list them as such**: `javis_panetitle.py --surface` is `type=int` compared to internal `surface_id` (:380, :55) → `--surface 50` no-ops on HQ; `master-send.sh` only accepts `surface:N` (:93). Both non-destructive, but master's CLAUDE.md example `apply --surface <번호>` will silently do nothing with a display number. — fix: add both to the "안 받는 곳" table; panetitle.py learns `#N` in ③(0) if desired.

**[LOW] §10-2 ③ — pin machine stdout lines.** `schedule.rs:1191` (`aiterm_parse`) reads launch-agent's last stdout line via `parse_surface_ref`, which accepts a bare number. If ③ ever prints a display number on the last line of `launch-agent`/`new-surface` (cys.rs:13207, :13231, :3015), schedule would treat it as an internal id. — fix: add a T3b-style source pin: last stdout line of `launch-agent`/`new-surface` stays `surface:N`.

**[LOW] §13 — line refs**: all cited handlers/state/recall/panetitle/lib/cys/main.ts lines re-verified at 526325bf and correct (handlers 318·3048·3064·3320·3457·3558·3932·4253·4334·5193·7394·7476; state 786·797·2071·2107·3000·3483·3502·3609·3865; recall 20-55·199·387·1371; panetitle 57·182·213·234·249·310·317; lib 2723; cys 317-331·365·845·1424·1443·3018·3743; main.ts 2396·3668·3790·7245). `cys list` at cys.rs:3018 is the one site the doc did not cite and should.

No finding against I0/I1/I2/I3/S within one daemon, the `#N` resolver semantics, TOCTOU (I0 makes a stale internal id land on "not found"), the 24h strict-less-than boundary, Windows state_dir (state.rs:2633-2660 verified), or exhaustion (seat still created).
