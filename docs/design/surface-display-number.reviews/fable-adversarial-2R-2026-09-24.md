# Fable 적대 서브에이전트 2R — 판정 원문 (개정판 d1cc6878 대상)

- 발주: worker-31@surface:1049 · 2026-09-24 08:0x · 모델 fable(서브에이전트 · 읽기 전용 · 도구 23회 · 약 622초)
- 아래는 서브에이전트 최종 보고를 **그대로** 옮긴 것이다(HTML 엔티티 `&amp;`·`&lt;`·`&gt;` 만 원래 글자로 되돌림).
- ⚠워커 판정: MED(셸 절단 → 폴백)는 **실측으로 기각**했다 — `shell-hash-probe-2026-09-24.txt` · 설계 문서 §15-2.

---

VERDICT: REVISE

FINDINGS:

`[HIGH] §6 · §3-2 표 「데몬 부팅」 · §10-1 recall.rs` — The 3-way seed (`UNION ALL … FROM surface_numbers`) runs before the table can exist, so the first 1.1.6 boot (and any DB lacking the table) seeds 0 and breaks I0 on the upgrade path. — evidence VERIFIED: `src/bin/cysd/recall.rs:387-408` `max_surface_id` uses a raw `Connection::open` + one `query_row(...).ok()…unwrap_or(0)`; the schema (`CREATE TABLE IF NOT EXISTS`) lives only in `open_db` (:24-55), which is first called inside the writer thread at `recall.rs:95`, spawned from `state.rs:3045` (`recall_tx`) — *after* the seed at `state.rs:3000` (`next_id: … max_surface_id(&socket_path)+1`). With the table absent, SQLite fails the whole UNION ("no such table") → `.ok()` → `None` → 0 → `next_id=1`. Consequences: §6's "1.1.5 → 1.1.6 첫 기동: 시드는 지금과 같다" is false as specified; the §3-2 boot-orphan UPDATE at :3000 also has no table; the new `surface.numbers_seed_failed` alarm ("DB 있는데 질의 실패") would fire on every fresh upgrade, burying real failures. — fix: (a) specify that Daemon construction runs the schema synchronously (call `open_db`/the sync dedicated connection) **before** the seed and before the orphan UPDATE; (b) make the seed tolerant per table (three separate `query_row`s, missing-table = 0 for that branch only) or return an enum `{Missing, Ok(max), Failed(err)}` so the alarm distinguishes "file absent" / "table absent (upgrade)" / "query failed"; (c) T2 adds "1.1.5 DB without `surface_numbers` → seed = MAX(lines,chains), no alarm" (mutant: UNION against missing table). Note the bus does not exist at :3000 (`EventBus::new` is the next field in the same literal), so publish the alarm after construction — the ring (`events.rs:10` `RING_CAPACITY=4096`) retains it for late subscribers.

`[MED] §4-2 · R-12 · §12 ④ · §16 D-1` — The corrected shell-`#` analysis covers only "flag after positional"; it misses the "surface flag followed by other flags" class, and one member exceeds the stated residual ("자기 좌석 입력 한정"). — evidence VERIFIED: `cys.rs` Clap audit — `ClaimRole { role: String (positional), surface: Option<String> #[arg(long)], takeover_empty_seat: bool #[arg(long)] }`; `run_claim_role` → `target_surface(&surface,&None)` → `CYS_SURFACE_ID` fallback (:1424-1439). So `cys claim-role master --surface #17 --takeover-empty-seat` (natural spelling for a takeover) reaches the daemon as `claim-role master` from the **caller's** pane. `handlers.rs:4861-4877`: for master/cso the deny fires only when a holder is live (`h != sid && !hs.exited`); when the holder is dead/absent — exactly when someone types takeover — the caller pane binds `master`. Effect: the role address rebinds, so every other agent's `--to master` traffic now lands on an unintended live pane. Same class: `Quiesce { surface, off }` → `cys quiesce --surface #17 --off` becomes `cys quiesce` → caller's own inbox injection turned **on**-hold (inverted). `SetStatus`, `feed push --surface #17 --wait` also fall back to self (benign). Safe (checked): `cycle-agent`/`reinject`/`node-recover` use `resolve_role_or_surface` → `Err("need --role or --surface")` when both are None; `resize` errors (rows/cols required); `run` errors (command required); `close-surface`/`reap-surface`/`queue clear`/`queue deliver`/`attach` are required positionals. — fix: §4-2 enumerate three ordering classes with the observed outcome per command (last-arg → clap error; flag-after-positional → self fallback: send/send-key/feed push/claim-role; flag-before-flags → self fallback **plus** the trailing flags vanish: claim-role, quiesce, set-status, feed push); rewrite R-12's residual as "자기 좌석 입력 **또는 역할 재바인딩(claim-role → 타 좌석의 --to 트래픽 유입)**"; T9 adds the claim-role and quiesce cases; D-1 권고의 단점 row must cite claim-role, which materially strengthens option B (shell-safe alias). Nit: "zsh 는 설정에 따라 다름" — non-interactive zsh (scripts) always comments; only interactive zsh needs `INTERACTIVE_COMMENTS`.

`[LOW] §4-1 ※ · T14 · R-21` — The `cys list` parser audit covers repo copies only; one user-pack script breaks on the `no=` token. — evidence VERIFIED: `~/.cys/pack/bin/javis_reconstruct_state.py:78-96` (`parse_cys_list`) sets `label = first column not prefixed by surface:/role=/pid=/exited=` → becomes `no=50` instead of the title. Also `~/.cys/pack/bin` diverges from the repo for `cys-dept`, `javis_boot_node.py`, `javis_formation.py`, `javis_phoenix.py`, `javis_preflight.py`, `javis_dept_request.py`; I checked the user copies of boot_node (:273-290 key/value guard `if "=" not in c: continue`), formation (:444-448 `f[1]`,`f[3]`), cys-dept (:826-829) — all index-safe. Non-destructive, but the doc's "9곳 그대로 동작" is stated as exhaustive. — fix: add `javis_reconstruct_state.py` to the list (skip tokens matching `^[a-z_]+=` generically), state the audit scope includes `~/.cys/pack/bin` custom/divergent copies, extend the T14 fixture.

`[LOW] §2-3 따름정리 S · §6 · R-3 · R-22` — S's proof uses I1 as "살았으면 보이는 번호도 n" but I1 permits 「없음」; in the I0-broken run the doc claims "I1 보호 규칙이 맨숫자 안전(S)을 지킨다" unconditionally, which does not follow. — evidence INFERENCE: seat n (id ≤ 999) live with "—" (§2-1 ④ fired ⇒ I0 broken); its blocker closes and 24h passes; a later seat Y (id > 999, ≥999 more creations in the same run) probes onto display n; a human reading "n · worker" on Y types bare `n` → internal n → **seat n, a different live pane**. Remote (needs I0 breakage + ≥999 creations + long-lived "—" seat), but the stated guarantee is false. — fix: state S's precondition (no live seat with id ≤ 999 and display 「—」), and add cheap hardening: when §2-1 ④ fires, set a per-run `numbers_suspended` flag so every subsequent seat is 「—」 until restart (then no Y can take n; `#N` refuses; S holds by construction). Alarm text says numbering is suspended.

`[LOW] §3-2 ②③ (lock scope · wording)` — (i) The close UPDATE is placed in the same "display_alloc 락 안" step as the memory update; a synchronous UPDATE with `busy_timeout` under the leaf lock (recall writer holds 1s batch transactions, `recall.rs:86~`) can stall seat creation for the full timeout on every reap/close. Since memory is authoritative and a lost close UPDATE degrades to boot_orphan (conservative), only the INSERT needs to be sync-under-lock. (ii) §3-2 ② says the lock is released after INSERT; PTY spawn is outside it (VERIFIED `state.rs:3502` fetch_add → :3511 openpty → :3654 spawn → :3822 surfaces lock; no other locks in between), so the "PTY 실패 → 되돌림(락 안)" row means a **re-acquire** — say so. Restore-to-prev race checked: safe (Live(id) blocks concurrent probes; close guard `holders[n].surface_id == id`; the seat is not in `surfaces` until :3823 so no concurrent close can touch it). — fix: name the `busy_timeout` value; move the close UPDATE outside `display_alloc` (or through the writer queue); write "락 재획득" in the PTY-failure row.

Checked and found correct (so absence is auditable): `no=` at index 4 against all repo parsers (`javis_awaken.py:214-224` kv/`cols[-1]`, `javis_bootstrap.py:2157-2161/2198-2202`, `javis_boot_node.py:274-289`, `javis_cycle_autopilot.py:658-666` `len<6`/`parts[-1]`, `javis_formation.py:444-448`, `javis_orchestra.py:2848-2854`, `javis_wakeup.py:195-202`, `javis_idle_audit.py:142-175` token regex, `javis_phoenix_harness.py:251`, `cys-dept:884-895`) and all live channel scripts (`cys-who.sh:28` awk `$2/$1`, `spawn-worker.sh:164-165` `pid=` scan/`$NF`, `master-live-tree-verify-guard.sh:114-116` `cols[-1]`, `scan-verify-backstop.sh:71` grep, `cso-active-monitor.sh:609` regex, `jarvis-who.sh` parses cmux TSV not cys); no `[4]`/`$5` title-by-index read anywhere (only `javis_cycle_verifier.py:685` = `cys feed list`, unrelated); no script derives a surface id from the title's leading number; `cys identify` is JSON and `spawn-worker.sh:45` reads `["caller"]["surface_ref"]`, so `caller_display_no` is additive; `display_no` is not an existing identifier in src/ui/pack; `close_surface` (`governance.rs:4885`) takes `surfaces` itself so no caller holds it (channels.rs/schedule.rs/handlers.rs sites) and the "surfaces 락을 푼 뒤 UPDATE" placement is feasible; `pk_conflict` vs `io` split and T3c stdout pin are correctly specified; `Surface.created_at: f64` (`state.rs:797`); `parse_surface_ref`/`resolve_surface_id` unchanged claim holds; source tree at HEAD d1cc6878 is identical to 526325bf for `src/ ui/ cysjavis-pack/` (docs-only commits).
