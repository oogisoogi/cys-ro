# Fable 적대 서브에이전트 3R — 판정 원문 (fe6c541d 대상 · 2R 개정 부분만)

- 발주: worker-31@surface:1049 · 2026-09-24 08:2x · 모델 fable(서브에이전트 · 읽기 전용 · 도구 13회 · 약 443초)
- 아래는 서브에이전트 최종 보고를 **그대로** 옮긴 것이다(HTML 엔티티 `&gt;`·`&lt;` 만 원래 글자로 되돌림).

---

**VERDICT: REVISE** (one MED spec-ordering bug in the round-2 boot sequence; everything else in scope holds)

**FINDINGS:**

`[MED] §3-2 「데몬 부팅」 ⑴⑵ · §6 표 만들기→시드 · T2 ⑷` — Step ⑴ opens the dedicated connection **before** the seed, and `Connection::open` creates the file, so the seed's `Missing` (= "파일 없음") branch can never be observed; on a fresh install (or any isolated test daemon: T5/T11 fixtures) the file now exists with only `surface_numbers` created, and the `lines`/`chains` branches fail with "no such table" → classified `Failed` → spurious `surface.numbers_seed_failed` on every fresh install. This is the same alarm-burying class the 2R HIGH fixed, reintroduced at the other end. — evidence VERIFIED: `recall.rs:390-392` (`if !path.exists() { return 0 }` is the only Missing test today); rusqlite 0.32.1 default flags include `SQLITE_OPEN_CREATE` (Cargo.lock); doc ⑴ names only `CREATE TABLE IF NOT EXISTS surface_numbers …`, not the full schema; T2 ⑷ asserts "경보 0" only for the upgrade fixture (lines/chains present), not for an absent file. — fix: make ⑴ run the **whole** existing schema (call `open_db`, `recall.rs:24-55`, exactly as 2R's fix text said) so every branch returns `Ok(0)` on a fresh file, and define `Missing` as "file absent **before** ⑴" (or drop `Missing` entirely). Add "fresh state_dir → seed 1 · 경보 0" to T2 ⑷ (mutant: ⑴ creates only surface_numbers → alarm fires → red). Side benefit: `PRAGMA journal_mode=WAL` is then set by the first connection, avoiding the sync connection creating a rollback-journal DB that the writer thread flips to WAL later.

`[LOW] §6 ⑵ · §3-2 ③ 부팅` — The only way §2-1 ④ can actually fire (I checked: with a readable table, `holders[n]`'s owner row `m` is in the table so seed ≥ m+1; with an unreadable table holders are empty) is "surface_numbers seed branch `Failed` (transient BUSY) while holders reconstruction / orphan UPDATE succeed" — a partial read that puts the run into permanent 번호 정지 from one transient. — INFERENCE. — fix: do ⑴⑵⑶ + holders reconstruction in one transaction on the dedicated connection; if the `surface_numbers` branch is `Failed`, leave holders empty (consistent with the seed) and say so. Also state whether every post-suspension seat re-emits the alarm or only the first (recommend once).

`[LOW] §2-3 번호 정지 · §5 resolve_display` — Refusing `#N` under suspension is stricter than S needs: `surface.resolve_display` resolves by display among **live** seats (I2 ⇒ unique), so `#k` for seats numbered before the fire is safe; refusing it only costs convenience. Harmless, but §5's error codes (`display_out_of_range` / `display_not_live`) have no code for the suspended refusal, and T9/T4 assert the refusal. — INFERENCE. — fix: either allow resolution (drop "`#N` 해석도 거부") or add a `display_suspended` code to §5.

`[LOW] §3-2 ② busy_timeout 근거` — "2초 · recall 쓰기 스레드의 1초 묶음 트랜잭션보다 길게" is mis-founded: the writer collects for 1 s **before** `BEGIN` (`recall.rs:126-141`), so the write lock is held only for the insert burst; the long holder is `maybe_prune`. Also rusqlite already sets a **5 s** default busy handler (`rusqlite-0.32.1/src/inner_connection.rs:119`), so "2초" is a reduction, not an addition. — VERIFIED. — fix: reword the rationale (prune is the holder) and either keep the default 5 s or justify 2 s.

`[LOW] §9 T14 · §10-2` — `javis_reconstruct_state.py` exists **only** in `~/.cys/pack/bin` (no repo copy — VERIFIED by `find`), so a repo unit test "T14 covers it" is machine-dependent. — fix: T14 tests a fixture copy of `parse_cys_list` or the check lives beside the user script; the parser fix itself (`^[a-z_]+=` skip) is correct — `no=50`/`no=-` are skipped, `tail[0]` becomes the title again, empty-title behavior unchanged.

**Checked and found correct (so absence is auditable):**
- Item 1 ordering: Rust evaluates struct-literal fields in source order, so ⑴⑵⑶ at `state.rs:3000` complete before `EventBus::new` (`:3001`) and `spawn_writer` (`:3045`); the W1 flock gate (`main.rs:1129-1141`) runs before `Daemon::new` (`:1277`) and the bind (`:1414`), so a losing second instance never runs the boot_orphan UPDATE against a live daemon's rows. Ring (`events.rs:10`, 4096) retains post-construction alarms.
- Item 2: `numbers_suspended` does what it claims for the reachable case. At fire time the blocker H cannot be live (a live H with display n would need internal id > 999 issued before id n ≤ 999 — impossible under in-run monotonic ids), so H is Closed-within-W; the only later threat is a wrapped Y taking n, which suspension blocks. Not persisted across restart — correct, since no seat survives restart (`state.rs:20-21` setsid+killpg) and "—" rows have NULL display so they never seed holders.
- Item 3: PTY-failure restore is race-free — `prev` can only be Closed/None (a live prev would have blocked the candidate), the close guard `holders[n].surface_id == id` cannot match another seat while `Live(id)` is held, and the seat is not in `surfaces` until `state.rs:3823`. Boot orphan vs W2 cold-boot restore (`main.rs:1420`, after bind) is correctly ordered. The single `fetch_add` issuer is `state.rs:3502` (no bypass path).
- Item 4 confirmed, previous Fable MED **refuted**: bash/non-interactive zsh/PowerShell drop from the word-initial `#` to end of line, leaving `--surface` valueless. Every `surface` option in `cys.rs` is `#[arg(long)] surface: Option<String>` (lines 66-1354, 23 sites) or a required positional (`CloseSurface:318`, `ReapSurface:330`, `Attach:365`, `Deliver:845`); grep finds **zero** `num_args`/`default_missing_value`/`require_equals`/`allow_hyphen_values` in cys.rs (the only `last = true` at :372 is `run`'s command vec). So clap must error before `target_surface` (`cys.rs:1424-1439`) can reach the `CYS_SURFACE_ID` fallback — matching all 7 probe results (rc=2). `--surface=#17` and `'#17'` survive the shell; interactive zsh without `INTERACTIVE_COMMENTS` passes `#17` literally, which is the intended path. R-12 / §12 ④ / D-1 closure are correct.
- Item 5: fix placement ("사용자본 · 가장 먼저") is right given no repo copy; T14 scope extension is correct modulo the LOW above.
