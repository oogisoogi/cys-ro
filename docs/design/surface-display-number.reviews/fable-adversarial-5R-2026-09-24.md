# Fable 적대 서브에이전트 5R — 판정 원문 (6fe43ab8 대상 · 4R 개정분만)

- 발주: worker-31@surface:1049 · 2026-09-24 08:3x · 모델 fable(서브에이전트 · 읽기 전용 · 도구 12회 · 약 381초)
- 아래는 서브에이전트 최종 보고를 **그대로** 옮긴 것이다(HTML 엔티티 `&lt;`·`&gt;` 만 원래 글자로 되돌림).

---

**VERDICT: REVISE** (one MED — a doc-internal contradiction the round-4 §2-3 rewrite created; the three mechanisms themselves hold)

**FINDINGS:**

`[MED] §2-1 ④ · §3-2 ⑤ · §6 ⑶ · R-3 · R-22` — Round 4 changed §2-3 to "I1 protection + suspension are unreachable through this design's own write path (unreadable table ⇒ holders empty ⇒ nothing to block; readable table ⇒ seed ≥ max+1 ⇒ no row can hold display=id with surface_id≠id)". That analysis is correct (I re-derived it: for ④ to fire on id≤999 you need a row with `display_no = id`, `surface_id ≠ id`, `surface_id < seed ≤ id`; own writes give id≤999 rows display∈{id,NULL} and id>999 rows only when seed>999). But five places still say the opposite — that ④/suspension are what protect S in exactly those own-path cases: §2-1 ④ "(대응표 쓰기 실패 등)", §3-2 ⑤ "이때도 I1 은 §2-1 ④ 보호 규칙이, S 의 전제는 §2-3 번호 정지가 지킨다", §6 ⑶ "그 실행에서는 §2-1 ④ I1 보호 규칙과 §2-3 번호 정지가 맨숫자 안전(S)의 전제를 지키고", R-3 "I1 보호 규칙이 「—」 를 주고 번호 정지로 … S 의 전제를 지킴", R-22 "I1 보호 + 번호 정지". In the seed_failed / write_io-then-restart scenarios ④ cannot fire (holders empty), so the doc names a defense that does not operate. Not a safety bug — S still holds there because holders start empty and in-run I1 holds by construction — but an implementer/test writer reading R-22 will look for a ④ firing that never comes, and T4's "재사용 주입 연쇄" is the only place it is exercised. — fix: rewrite those five spots to "S holds because holders are empty/in-run I1 holds by construction; the signal is `pk_conflict`; ④/suspension are the external-corruption fence only (§2-3)".

`[LOW] §3-2 ②/③ · §2-1 ②⑴ · R-14` — "blocked ⑴" is worded as "아직 좌석 목록에 있다" while the round-4 restore argument ("`Live(id)` 가 다른 할당의 탐색을 막고 … surfaces 에 들어가기 전(:3823)") only holds if blocked is decided by `holders[n].state`, never by a `surfaces` lookup. In the alloc→INSERT→openpty→spawn→`surfaces.insert` window (login-shell spawn, tens–hundreds of ms) the seat is Live in holders but absent from the map; the close window is the mirror. T1's pure signature `pick_display(id, holders, now, w)` already implies holders-only. — fix: one clause in §2-1 ② or §3-2 ③: "막힘 판정은 holders 상태만 본다(surfaces 맵 조회 금지)".

`[LOW] T13 · T2 ⑹` — With the read-only full-schema fixture, boot ⑶ UPDATE emits one `write_io` (no `surface_id`) at statement start even with 0 orphan rows (OP_Transaction write flag runs before any row scan), and every seat INSERT then emits another. T13 "좌석 쓰기마다 write_io" and T2 ⑹ "write_io 1회" are both true only if the assertion filters on `surface_id` present/absent; T2 ⑹ additionally creates seats to check "그 번호들 막힘", which adds seat-level `write_io`s. — fix: state in both tests that the boot alarm is identified by `surface_id` absent, and count seat alarms by `surface_id` present. (§5 table could also carry "부팅당 1회" on `seed_failed` and boot `write_io`, matching §3-2 ⑷.)

`[LOW] §3-2 ⑤ + PTY-failure row` — If INSERT fails with `pk_conflict` (a pre-existing row for this id) and PTY then fails, the `spawn_failed` UPDATE rewrites the old row's `closed_at`/`close_kind`; at the next boot that row is skipped for holders (spawn_failed excluded), un-blocking the old row's display early if it was inside W. Same for the later `close` UPDATE (harmless — conservative). Only reachable after an I0 break, so external/corruption territory. — fix: on `pk_conflict`, skip this seat's later UPDATEs (or make them `WHERE surface_id=? AND created_at=?`).

**What I checked (base 526325bf, read-only):**

- Item 1 — `create_surface_with_env` (`src/bin/cysd/state.rs:3483-3870`): `fetch_add` at :3502 is immediately followed by `openpty` at :3503; nothing between them publishes, replies, spawns, or writes env. The first id escapes are `builder.env(CYS_SURFACE_ID)` (:3627ff), `wire_seat_profile_before_exec` (:2717, publishes an event with `Some(surface_id)` on failure — after openpty), `spawn_command` (:3654), `surfaces.insert` (:3823), `surface.created` publish (:3865), RPC reply in `handlers.rs:3294ff`. So an INSERT placed before :3503 precedes every escape; I0 holds. Two concurrent creates serialize on `display_alloc` (fetch_add now inside the lock). Create-vs-close on the same number: close only writes `holders[n]` when `holders[n].surface_id == id`, and a live holder blocks, so a foreign seat cannot overwrite. PTY-failure restore: between alloc and re-acquire nothing can touch `holders[n]` (creates are blocked by `Live(id)`; `close_surface` is the only removal point — `governance.rs:4891` is the sole `surfaces.remove` in cysd — and it requires the id to be in the map, which it isn't yet); the "still Live(id)" guard is therefore always true and harmless. INSERT failure with memory kept keeps in-run I2. The lock-order claim holds: the only non-test caller is `handlers.rs:3294`, whose surrounding comment states it is a lock-free region; `close_surface` drops `surfaces`/`roles` before its first I/O, so the holders update + UPDATE slot in after the block at :4926.
- Item 2 — `open_db` (`recall.rs:24-55`) is `PRAGMA journal_mode=WAL` + `CREATE … IF NOT EXISTS` only, no writes on an already-WAL full-schema file, so T2 ⑹/T13 fixtures reach ⑶ as described. Seed query (`recall.rs:387-408`) is `MAX(surface_id)` with no `close_kind` filter, so `spawn_failed` rows (whose id may have escaped via `wire_seat_profile_before_exec`'s event) still raise the seed — consistent with holders excluding them. `Daemon` literal order (`state.rs:3000` next_id → :3001 bus → :3045 `spawn_writer`) means no second writer exists during ⑶, so the deferred read snapshot then UPDATE on one connection cannot hit BUSY_SNAPSHOT. ⓪/⑴/⑵/⑶ failure branches are mutually consistent with §6 ⑶ (⓪ is the tiebreaker for fresh file + ⑴ failure) and with T2 ⑷⑸ and R-23; the only contradictions are the stale attribution sentences in the MED above.
- Item 3 — Reachability analysis correct (derivation above). Keeping ④ + suspension is not harmful on any normal path: it cannot fire from own writes, including the seed_failed, write_io-then-restart, boot_orphan, and 999-crossing paths. When it does fire (hand-edited row, or an older backup restored where a dead row with `display=id` closed inside W blocks the reissued id), it disables numbering for that run and re-fires on later boots only while ids stay ≤999 — self-limiting, and the seat is still created (4군 ③ intact). Cost claim (one branch + one flag) is accurate.
