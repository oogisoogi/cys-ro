BASE rc 0 ['0']
KILLED M1 좌열 모양 가드 제거 ← 2: ⑴ 좌열 보존(오너 ①) > master·cso 가 좌우로 나란한 저장 배치(옛 판·창 옮기기) → 좌열로 보지 않고 표준 배치(헤드리스 c18 전체 실행 적발)
KILLED M2 D1 C→A(규칙값 재계산 안 함) ← 5: ⑷ 재현된 결함이 사라진다 > R1 HQ 첫 배치 뒤 워커 입양 → 재배치됨 · 좌열 = 새 워커 수의 규칙값 · 워커 균등(종전 = 새 워커 0.50 · mas
KILLED M3 D1 C→B(사람 값도 규칙값으로) ← 10: ⑴ 좌열 보존(오너 ①) > 사람이 끈 좌열 몫(0.4)과 위아래 비율(0.6)이 열기·닫기 뒤에도 그대로
KILLED M4 허용 오차 0.01→0.2 ← 8: ⑴ 좌열 보존(오너 ①) > 사람이 끈 좌열 몫(0.4)과 위아래 비율(0.6)이 열기·닫기 뒤에도 그대로
KILLED M5 좌열 서브트리 보존 안 함 ← 3: ⑴ 좌열 보존(오너 ①) > 사람이 끈 좌열 몫(0.4)과 위아래 비율(0.6)이 열기·닫기 뒤에도 그대로
KILLED M6 remove 무시 ← 9: ⑷ 재현된 결함이 사라진다 > R2 워커 3 중 가운데 닫기 → 남은 둘 균등(종전 = 1/3 : 2/3)
KILLED M7 after 무시(늘 끝) ← 1: ⑵ 나머지 좌석 한 줄 균등(오너 ②) > 분할 = 대상 바로 다음 순서에 선다
KILLED M8 정확 덮기 검사 제거 ← 1: ⑴ 좌열 보존(오너 ①) > 좌열 이상 위치(master·cso 가 세로 열 안에 워커를 품음 · 그 열 몫 0.6) → 그 몫을 이어받지 않고 표준 배치
KILLED M9 세로 분할 길 허용 ← 1: ⑴ 좌열 보존(오너 ①) > 좌열 이상 위치(좌열이 위아래 분할 속 · 비율이 규칙값과 다름) → 표준 배치로 폴백(세로 비율을 가로 몫으로 오독하지 않는다)
KILLED M11 균등 comb → 0.5 감싸기 ← 13: B16 오너 확정 비·방향 > worker 는 오른쪽 열에 들어가고 늘어나면 오른쪽으로 분할된다
KILLED M13 좌열 몫 가지 반대로 ← 15: ⑷ 재현된 결함이 사라진다 > R2b HQ 기기 가운데 워커 닫기 → 좌열 몫 그대로 · 남은 워커 균등
KILLED M14 좌열 없을 때 master/cso 순서 뒤집기 ← 15: ⑴ 좌열 보존(오너 ①) > master 만 있다가 cso 가 들어오면 좌열 = master(위):cso(아래) 4:1 · 몫 그대로
KILLED M15 새 창 0.5 감싸기 복귀 ← 2: 호출부 — 열기·닫기 9경로 + 정렬 단추가 같은 함수 > 새 창 → arrangeWs · 트리를 손으로 감싸거나 잘라내지 않는다
KILLED M16 detachPane replaceNode 복귀 ← 3: B16 결선 — 닫기가 먼저, 배치가 나중 > 배선이 그 순서다 — 스윕이 detachPane(닫기+배치)을 부르고 입양 배치는 그 뒤
KILLED M17 틱 역할 표 갱신 제거 ← 1: 호출부 — 열기·닫기 9경로 + 정렬 단추가 같은 함수 > 역할 표는 틱·복원·정렬 세 곳에서 채운다(끝난 좌석 포함)
KILLED M18 정렬 단추 standard 누락 ← 2: B16 호출부 — 3경로가 같은 함수를 부른다 > 입양·복원 입양·정렬 세 곳 전부 arrangeWs(autoArrange)를 경유
KILLED M19 역할 표 키 소켓 누락 ← 1: 호출부 — 열기·닫기 9경로 + 정렬 단추가 같은 함수 > 단일 입구 arrangeWs = autoArrange 한 줄
KILLED M20 틱 입양 배치 호출 제거 ← 4: B16 호출부 — 3경로가 같은 함수를 부른다 > 입양·복원 입양·정렬 세 곳 전부 arrangeWs(autoArrange)를 경유
KILLED M21 복원 입양 배치 제거 ← 3: B16 호출부 — 3경로가 같은 함수를 부른다 > 입양·복원 입양·정렬 세 곳 전부 arrangeWs(autoArrange)를 경유
KILLED M22 머리 × 배치 제거 ← 2: 호출부 — 열기·닫기 9경로 + 정렬 단추가 같은 함수 > 창 머리 × → arrangeWs · 트리를 손으로 감싸거나 잘라내지 않는다
KILLED M23 메뉴 「아래」 복귀 ← 1: 권고 C — 창 만들기는 상단에서 빠지고 전문가 칸에 > 누르면 오른쪽 한 갈래(「아래」 항목 0) — 종전 Split 과 같은 함수 · 단축키 유지
KILLED M24 F1 직계 조건 제거 ← 2: Opus 적대 1R 봉합 — F1 깊은 길 몫 · F4 좌열 비율 > F1 R1 잔재 트리(옛 입양 0.5 감싸기 · 좌열 0.25) → 사람 값으로 보지 않고 
KILLED M25 F1 끌기 범위 제거 ← 2: Opus 적대 1R 봉합 — F1 깊은 길 몫 · F4 좌열 비율 > F1 끌기 범위 경계: 좌열 몫 0.14 → 표준 몫
KILLED M26 F4 손상 비율 거부 제거 ← 4: Opus 적대 1R 봉합 — F1 깊은 길 몫 · F4 좌열 비율 > F4 손상된 좌열 위아래 비율(NaN) → 좌열로 보지 않고 표준
KILLED M27 F4 승격 비율 이어받기 제거 ← 2: Opus 적대 1R 봉합 — F1 깊은 길 몫 · F4 좌열 비율 > F4 첫 master 가 닫히고 master-2 가 올라와도 사람이 바꾼 위아래 비율(0.6
KILLED M28 F3 무변화 가드 제거 ← 1: 호출부 — 입양은 런타임이 선 그 자리에서 트리에 붙는다(Opus 적대 1R F2) > 이미 붙은 좌석을 또 붙이거나 없는 좌석을 닫으면 배치 무변경(정렬 단추만
KILLED M29 F3 × 탭 = current() ← 1: 호출부 — 입양은 런타임이 선 그 자리에서 트리에 붙는다(Opus 적대 1R F2) > 창 머리 × = 그 창을 품은 탭을 짠다(기다리는 사이 탭을 옮겨도)
KILLED M30 F2 입양 배치를 await 뒤로 ← 2: B3 호출부 — 입양 루프가 실제로 재배치를 부른다 > refreshPaneTitles 의 입양 블록 뒤에서 arrangeWs 를 입양 ws 에만 적용 · 0.5
KILLED M32 역할 표 이어 기억 제거 ← 1: 호출부 — 입양은 런타임이 선 그 자리에서 트리에 붙는다(Opus 적대 1R F2) > 역할 표는 목록에서 사라져도 화면에 남은 좌석의 역할을 이어서 기억한다(유
killed 29/29
